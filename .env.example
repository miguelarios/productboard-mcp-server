# Productboard MCP Server Configuration

# Server Configuration
MCP_SERVER_PORT=3000
MCP_SERVER_HOST=localhost
MCP_SERVER_TIMEOUT=30000

# Authentication Configuration
# Options: bearer, oauth2
PRODUCTBOARD_AUTH_TYPE=bearer

# Bearer Token Authentication
# Get your API token from: https://your-subdomain.productboard.com/settings/api-keys
PRODUCTBOARD_API_TOKEN=your_api_token_here

# OAuth2 Authentication (if using OAuth2)
PRODUCTBOARD_OAUTH_CLIENT_ID=your_client_id
PRODUCTBOARD_OAUTH_CLIENT_SECRET=your_client_secret
PRODUCTBOARD_OAUTH_REDIRECT_URI=http://localhost:3000/callback

# OAuth2 Scope Configuration - Choose ONE approach:

# OPTION 1: Use official Productboard role preset (RECOMMENDED)
# Available presets: contributor, maker, admin, custom
PRODUCTBOARD_OAUTH_PRESET=contributor

# OPTION 2: Override with custom space-separated scopes (advanced users)
# PRODUCTBOARD_OAUTH_SCOPES=users:read product_hierarchy_data:read notes:create

# Official Productboard Role Descriptions:
# - contributor: Most restricted - can create notes and access user info
# - maker: Limited permissions - can create releases, notes, and manage users
# - admin: Full access - comprehensive product hierarchy and system management
# - custom: Modify the 'custom' preset in oauth2-config.yml

# API Configuration
PRODUCTBOARD_API_BASE_URL=https://api.productboard.com
PRODUCTBOARD_API_TIMEOUT=10000

# Rate Limiting
RATE_LIMIT_GLOBAL=100
RATE_LIMIT_WINDOW_MS=60000

# Cache Configuration
CACHE_ENABLED=true
CACHE_TTL=300
CACHE_MAX_SIZE=100

# Logging
LOG_LEVEL=info
LOG_PRETTY=true

# Development
NODE_ENV=development